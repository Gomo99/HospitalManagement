@model Consumable
@{
    ViewData["Title"] = "Consumable Details";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Consumable Details</h1>
        <div>
            <a asp-action="EditConsumables" asp-route-id="@Model.ConsumableId" class="btn btn-warning me-2">
                <i class="fas fa-edit"></i> Edit
            </a>
            <a asp-action="ListOfConsumables" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Consumable Information</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Consumable ID:</dt>
                <dd class="col-sm-9">@Model.ConsumableId</dd>

                <dt class="col-sm-3">Name:</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">Type:</dt>
                <dd class="col-sm-9">
                    <span class="badge bg-info">@Model.Type</span>
                </dd>

                <dt class="col-sm-3">Quantity:</dt>
                <dd class="col-sm-9">
                    <span class="badge bg-@(Model.Quantity > 10 ? "success" : Model.Quantity > 0 ? "warning" : "danger")">
                        @Model.Quantity units
                    </span>
                </dd>

                <dt class="col-sm-3">Expiry Date:</dt>
                <dd class="col-sm-9">
                    <span class="@(Model.ExpiryDate < DateTime.Now.AddDays(30) ? "text-danger fw-bold" : "")">
                        @Model.ExpiryDate.ToString("yyyy-MM-dd")
                        @if (Model.ExpiryDate < DateTime.Now)
                        {
                            <span class="badge bg-danger ms-2">EXPIRED</span>
                        }
                        else if (Model.ExpiryDate < DateTime.Now.AddDays(30))
                        {
                            <span class="badge bg-warning ms-2">EXPIRING SOON</span>
                        }
                    </span>
                </dd>

                <dt class="col-sm-3">Description:</dt>
                <dd class="col-sm-9">@(string.IsNullOrEmpty(Model.Description) ? "No description" : Model.Description)</dd>

                <dt class="col-sm-3">Status:</dt>
                <dd class="col-sm-9">
                    <span class="badge bg-@(Model.ConsumableStatus == Status.Active ? "success" : "danger")">
                        @Model.ConsumableStatus
                    </span>
                </dd>

              
            </dl>
        </div>
    </div>

    <div class="mt-3">
        <a asp-action="ListOfConsumables" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to Consumables List
        </a>
    </div>
</div>